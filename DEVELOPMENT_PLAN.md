# Development Plan

计划以个人独立开发为前提，控制在 **46 个开发日** 内完成一个可用 Web MVP，并预留 5 个开发日缓冲。iOS 原生端不进入本轮主交付，只做前置验证和架构预留。

## D1-D5：项目整理与基线

目标：把当前脚本原型整理成可持续开发的项目。

- [~] D1：部分完成。已确定目录结构，`doc2md`、`convert`、`any2html`、`html2screen` 已位于 `src/`；已新增 `docs/` 和 `tests/`，测试样例文件尚未补齐。
- [x] D2：已使用 `uv` 统一依赖管理，根目录补充 `pyproject.toml`，运行依赖集中声明。
- [x] D3：已将图片输出整合到 `html2screen --img` 和主 pipeline `convert --img`，支持输入、输出、宽度、格式。
- [x] D4：建立样例文档集，覆盖 Markdown、TXT、DOCX、PDF、图片。
- [ ] D5：记录当前转换耗时，形成性能基线表。
- [ ] D5.1：定义性能基线场景，至少覆盖 Markdown、TXT、DOCX、PDF 到 HTML/PDF/WeChat PDF/png/jpeg 的典型输入样本与固定参数。
- [ ] D5.2：编写本地基线采集脚本或命令清单，统一记录单次运行耗时、输出文件大小、页面/图片尺寸和是否成功。
- [ ] D5.3：分别在本地开发环境与 Docker 环境采集首轮基线，区分 HTML 生成、PDF 渲染、长图渲染三类耗时。
- [ ] D5.4：形成性能基线表，落盘到 `docs/`，记录测试日期、机器环境、输入样本、命令参数、耗时结果和异常说明。
- [ ] D5.5：定义后续性能回归口径，明确 D27 复测时的对比字段、通过阈值和回归判定标准。

交付物：可重复运行的脚本入口、样例文件、性能基线。

## D6-D12：转换核心抽取与 pipeline 拆分

目标：从脚本转向可复用的转换模块，并把 pipeline 拆成 `any2html` 与 `html2screen` 两个阶段。

- [x] D6：已定义两阶段接口：`any2html` 负责将输入转为 HTML 中间产物，`html2screen` 负责将 HTML 输出为 A4 PDF、WeChat PDF、HTML、png/jpeg。
- [x] D7：已抽取 Markdown -> HTML 核心函数到 `any2html/markdown.py`。
- [x] D8：已建立 `any2html` 预处理模块边界，后续可接入 DOCX/PDF/TXT。
- [x] D9：已抽取 HTML -> A4 PDF、WeChat PDF、长图渲染接口到 `html2screen/renderers.py`。
- [x] D9a：保留 `doc2md` 作为独立文档到 Markdown 转换工具，后续可复用到 DOCX/PDF/TXT 的 `any2html` 接入。
- [x] D10：已设计中间文档结构，覆盖标题、段落、列表、表格、图片、代码块，并记录在 `docs/intermediate-document.md`。
- [x] D11：已实现快速预览接口和 `preview` 子命令，支持按 Markdown 块数生成轻量 HTML，并限制表格行数和代码块行数。
- [~] D12：已补充基础单元测试，覆盖 Markdown HTML 生成、表格包装、语言检测、CLI 输入发现和输出路径规则；转换结果快照尚未补齐。

交付物：`any2html`、`html2screen`、pipeline 雏形、快速预览接口、基础测试。

## D13-D20：Web MVP

目标：完成最小可用 Web 应用。

- [x] D13：已搭建 Web 前端和 FastAPI 服务骨架，提供 `web` 子命令、移动优先 Web shell、`/api/health`、`/api/version` 和 OpenAPI。
- [x] D14：已实现单文件上传和格式识别，提供 `POST /api/files`，返回 `file_id`、文件大小、扩展名、识别类型和支持状态。
- [x] D14.1：已实现鼠标拖拽上传文件，拖放后复用同一个 `POST /api/files` 上传链路。
- [x] D15：已实现 Markdown/TXT 预览，提供 `GET /api/previews/{file_id}`，上传后 Web 页面自动展示轻量 HTML 预览。
- [x] D16：已实现 DOCX/PDF 预览，DOCX 从段落和标题提取 Markdown，PDF 从页面文本提取 Markdown，并复用轻量 HTML 预览链路。
- [x] D17：已实现移动端阅读视图和基础暖纸主题，预览区支持进入/退出阅读模式。
- [x] D18：已实现 HTML/PDF 导出，提供 `GET /api/exports/{file_id}/html` 和 `GET /api/exports/{file_id}/pdf`。
- [x] D18.7：已将页面提示性文案和按钮默认改为中文，页首右侧提供醒目的 EN/中文切换控件，前端使用静态中英文字典切换页面文案，不接入外部翻译 API。
- [x] D18.8：已创建当前 MVP 的最小化静态展示 demo 网站；后续已收敛为单一前端源 `src/web/static/`，避免 `demo-site/` 与 Web 静态目录重复维护。
- [x] D18.9：已制定并实现 Docker 部署配置，面向 Alibaba Cloud Linux 4 容器优化版，用 Nginx 反代 FastAPI 后端和其内置静态前端。
- [~] D19：实现“微信阅读”与“飞书协作”导出预设。
- [x] D19.1：接入 Web 后端微信阅读 PDF 导出，提供 `GET /api/exports/{file_id}/wechat-pdf`，输出 `{stem}.wechat.pdf`，复用 `render_pdf(..., wechat=True)` 并写入上传文件同目录。
- [x] D19.2：接入 Web 后端长图导出，提供 `GET /api/exports/{file_id}/image?screen=small|large&format=png|jpeg`，小屏固定 430 CSS px，大屏固定 1080 CSS px，输出 PNG/JPEG 并写入上传文件同目录。
- [x] D19.3：更新 Web 导出配置控件，新增“小屏/大屏”“生成长图”“图片格式 PNG/JPEG”，生成长图点亮时 HTML/PDF 控件禁用，点灭后恢复。
- [x] D19.4：实现导出决策规则：长图开启时按屏幕预设和图片格式导出；长图关闭且 PDF+小屏导出微信阅读 PDF；长图关闭且 PDF+大屏导出标准 PDF；HTML 导出不区分小屏/大屏。
- [ ] D19.4a：实现导出仿真预览模式，预览窗根据当前导出配置切换小屏/大屏宽度、长图预览宽度和 PDF 预览样式，显式区分小屏微信阅读 PDF 与大屏标准 PDF 的版式效果。
- [ ] D19.4b：在预览区增加当前仿真模式提示，至少覆盖“小屏长图预览”“大屏长图预览”“微信阅读 PDF 预览”“标准 PDF 预览”和默认 HTML 预览。
- [ ] D19.4c：定义导出仿真预览边界，明确其仅模拟宽度与版式倾向，不承诺与最终 PNG/JPEG/PDF 像素级一致，并补充相应文案提示。
- [ ] D19.5：实现飞书协作 Markdown 预设，提供 `GET /api/exports/{file_id}/feishu-markdown`，输出 `{stem}.feishu.md`，作为后续飞书云文档导入任务的主中间格式。
- [ ] D19.6：实现飞书协作 HTML 预设，提供 `GET /api/exports/{file_id}/feishu-html`，输出 `{stem}.feishu.html`，用于人工复制、浏览器打开和飞书云文档粘贴验证。
- [ ] D19.7：补充 D19 导出测试和文档，覆盖新增路由、响应类型、文件名、同目录写入、unsupported type 415、前端 JS 语法检查和 Docker 长图能力依赖说明。
- [ ] D20：实现处理去向选择：阅后即焚、保存原文件、保存转换结果。

交付物：本地可运行 Web MVP，支持上传、预览、导出和去向选择。

## D21-D27：速度优化与任务状态

目标：解决核心痛点，让用户快速看到结果。

- [ ] D21：拆分快速预览任务和完整导出任务。
- [ ] D22：增加任务状态：等待、处理中、成功、失败。
- [ ] D23：增加任务进度和错误详情。
- [ ] D24：实现文件指纹缓存，重复文件避免完整重跑。
- [ ] D25：实现大文件分页预览或前几页预览。
- [ ] D26：增加任务取消，支持阅后即焚时停止未完成工作。
- [ ] D27：复测性能基线，记录优化前后差异。

交付物：快速预览链路、任务状态、缓存和取消能力。

## D28-D34：微信与飞书场景优化

目标：验证真实分发场景，而不是只在浏览器里可用。

- [ ] D28：优化微信阅读 PDF 的版式、文件名和大小。
- [ ] D29：验证文件传输助手、微信群、微信收藏路径。
- [ ] D30：优化微信内置浏览器预览页面。
- [ ] D31：优化飞书云文档复制的 HTML/Markdown 结构。
- [ ] D31.1：验证 `.feishu.md` 导入飞书云文档后的标题、列表、表格、代码块和图片占位表现，并反向调整飞书协作 Markdown/HTML 输出结构。
- [ ] D32：验证飞书聊天文件、飞书内置浏览器、云文档粘贴。
- [ ] D32.1：设计飞书账号登录方案，覆盖 Web 与微信小程序的 OAuth/账号绑定流程、token 生命周期、安全存储、退出登录和小程序非 DOM 环境约束。
- [ ] D32.2：设计并实现飞书云文档导入 API，目标链路为飞书登录后上传 Markdown 文件、创建导入任务、轮询任务结果并返回云文档链接。
- [ ] D32.3：将飞书导入过程接入任务状态体系，覆盖等待、处理中、成功、失败、错误详情和重试/降级策略。
- [ ] D33：实现短期预览链接或本地二维码预览方案。
- [ ] D34：整理微信/飞书兼容性测试清单。

交付物：微信/飞书适配记录、导出预设、兼容性清单。

## D35-D39：隐私、存储与清理

目标：让“阅后即焚”和本地保存策略可信。

- [ ] D35：实现临时文件目录和生命周期管理。
- [ ] D36：实现阅后即焚清理逻辑，覆盖源文件、中间文件、导出文件。
- [ ] D37：实现手动删除和自动过期清理。
- [ ] D38：规范保存原文件和保存转换结果的目录结构。
- [ ] D39：补充隐私提示、错误提示和危险操作确认。

交付物：临时文件清理、阅后即焚、本地保存策略。

## D40-D46：打磨、测试与发布准备

目标：形成可试用版本。

- [ ] D40：补齐端到端测试样例。
- [ ] D41：处理异常文件、损坏文件、超大文件的错误路径。
- [ ] D42：优化移动端 UI 细节和加载状态。
- [ ] D43：整理 README、架构文档和开发记录。
- [ ] D44：打包本地启动脚本或 Docker 开发环境。
- [ ] D45：准备产品服务器部署方案，覆盖生产环境配置、反向代理、HTTPS、进程守护、日志、上传目录、临时文件清理和备份策略。
- [ ] D45.1：制定域名与 HTTPS 接入方案，覆盖备案限制、域名解析、80/443 入口、证书申请与自动续期、Nginx 反代到 Docker 服务、微信内置浏览器访问验证。
- [ ] D46：发布第一个 Web MVP 试用版本。

交付物：可试用 Web MVP、文档、测试样例、启动方式、服务器部署方案。

## D47-D51：缓冲与下一阶段评估

目标：处理不可预期问题，并决定后续路线。

- [ ] 修复 MVP 试用中暴露的问题。
- [ ] 评估是否需要引入后台队列、对象存储或账号系统。
- [ ] 评估 iOS 原生端必要性。
- [ ] 评估飞书机器人/Webhook、小程序、PWA 的优先级。
- [ ] 制定第二阶段开发计划。
